#include <stdio.h> /* The include command defines the library. The printf command is within the <stdio.h> library, so we include it. */
int main (void) /* This is required in every C program. */
{    /* This brace opens a block. To close it, the opposite brace must be used. */
 printf("Hello"); /* This is a function. Since it's not an original function, it is called a SubRoutine. It outputs the text within the quotes. */
  return(0); /* Even if not written, the system automatically adds it. It indicates the end and that the program should return to the operating system. */
} /* The block is closed here. */
